use std::path::Path;

use anyhow::Result;

/// # Errors
///
/// Returns an error if the restore operation fails.
#[allow(clippy::todo)]
pub async fn handle_restore(_dir: &Path) -> Result<i32> {
    todo!()
}
