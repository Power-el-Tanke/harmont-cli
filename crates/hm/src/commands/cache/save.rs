use std::path::Path;

use anyhow::Result;

/// # Errors
///
/// Returns an error if the save operation fails.
#[allow(clippy::todo)]
pub async fn handle_save(_dir: &Path) -> Result<i32> {
    todo!()
}
